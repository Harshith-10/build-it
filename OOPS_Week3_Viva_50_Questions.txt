1. What is a loop in programming and why do we use loops?
2. What are the three main types of loops available in Java?
3. What is a for loop and what is its syntax in Java?
4. What is a while loop and when should you use it over a for loop?
5. What is a do-while loop and how does it differ from a while loop?
6. What is a loop control variable or index variable?
7. What is an infinite loop and how does it occur?
8. What is the increment (++) and decrement (--) operator in loop counters?
9. What is a lowerbound and upperbound in a loop sequence?
10. What is an accumulation variable (like sum = sum + number)?
11. What is factorial of a number N?
12. What formula is used to calculate factorial of 5 (1 * 2 * 3 * 4 * 5)?
13. What is a harmonic series in mathematics?
14. What is the value of Pi (π) approximately in Java (Math.PI)?
15. What does the modulo operator (%) do when checking multiples of 3, 5, or 7?
16. What is a Fibonacci sequence?
17. What are the first two numbers in a standard Fibonacci sequence?
18. What is digit extraction from an integer?
19. Which operator extracts the last digit of an integer in Java (% 10)?
20. Which operator drops/removes the last digit of an integer in Java (/ 10)?
21. What is the result of integer division 1 / 2 in Java?
22. How do you convert integer division to floating-point division in Java?
23. What is the purpose of final keyword when defining loop boundaries like LOWERBOUND = 1?
24. What is a counter variable in a loop?
25. How do you compute the average of numbers summed in a loop?

26. In SumAverageRunningInt, how does the for loop calculate the sum of 1 to 100?
27. In SumAverageRunningInt, why is average calculated as (double) sum / 100?
28. How would you convert the for loop in SumAverageRunningInt into a while loop?
29. How would you convert the for loop in SumAverageRunningInt into a do-while loop?
30. In Product1ToN, why does computing factorial of 13 cause an integer overflow in 32-bit int?
31. In Product1ToN, why should we use long instead of int to compute factorial of 13 or 14?
32. What is the maximum factorial value that can be accurately stored using a 64-bit long?
33. In HarmonicSum, why is summing from right-to-left (n to 1) more accurate than left-to-right (1 to n)?
34. How does floating-point loss of precision occur when adding very small double numbers to large numbers?
35. In ComputePI, how does the series (1 - 1/3 + 1/5 - 1/7 + ...) approximate the value of Pi?
36. In ComputePI, how do we alternate between adding (+) and subtracting (-) terms using % 4?
37. In CozaLozaWoza, what condition checks if a number is divisible by both 3 and 5?
38. In CozaLozaWoza, why can't we use a nested if-else-if structure for multiples of 3, 5, and 7?
39. In Fibonacci, how do you compute F(n) using previous terms fnMinus1 and fnMinus2?
40. In Fibonacci, how do you update fnMinus1 and fnMinus2 in each iteration of the loop?
41. In ExtractDigits, what is the output when extracting digits of 15423 in reverse order?
42. In ExtractDigits, why does the loop condition use (number > 0) as its termination test?
43. What is the difference between pre-increment (++i) and post-increment (i++) inside a loop header?
44. How do you modify SumAverageRunningInt to sum only the even numbers from 1 to 100?
45. How do you modify SumAverageRunningInt to find the sum of squares (1*1 + 2*2 + ... + 100*100)?
46. What is a Tribonacci sequence and how does it differ from a Fibonacci sequence?
47. How many times does a do-while loop execute if its condition is initially false?
48. What is the value of factorial of 0 in Java?
49. Why does dividing double / int produce a double result in Java?
50. How do you calculate the absolute difference between two double sums using Math.abs()?
