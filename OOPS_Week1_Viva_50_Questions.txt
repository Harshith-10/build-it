1. What is the name of the main method in Java where program execution begins?
2. Why is public static void main(String[] args) required in every Java application?
3. What extension is used when saving a Java source code file?
4. What extension is generated when a Java file is compiled?
5. What is JVM and what is its main function?
6. What is the difference between JDK and JRE?
7. What is CamelCase naming convention in Java?
8. How should class names be written according to Java naming conventions?
9. How should method and variable names be written in Java?
10. What is a primitive data type? List 4 primitive data types used in Java.
11. What is the purpose of the if-else statement in Java?
12. What does the modulus operator (%) do in Java?
13. Which operator is used to check if two numbers are equal in Java?
14. Which operator is used to check if a value is greater than or equal to another in Java?
15. What is the purpose of the switch statement in Java?
16. What is the use of the break statement in a switch case?
17. What happens if you forget to put a break statement in a switch case?
18. What is the role of the default block in a switch statement?
19. What is the difference between System.out.print() and System.out.println()?
20. What are single-line comments and multi-line comments in Java?
21. What is a boolean data type and what two values can it hold?
22. What is the assignment operator (=) used for in Java?
23. What is the difference between = and == in Java?
24. How do you declare an integer variable named mark with a value of 49 in Java?
25. What is a String in Java and how is it enclosed in code?

26. In the CheckPassFail program, what condition is checked to print "PASS"?
27. In CheckPassFail, what output is displayed if the mark is 49?
28. In CheckPassFail, what output is displayed if the mark is 50?
29. In the CheckOddEven program, how do we test if a number is even using the % operator?
30. In CheckOddEven, what condition checks if a number is odd?
31. What will the CheckOddEven program print if the input number is 0?
32. In the PrintNumberInWord program, what output is displayed when number is 5?
33. In PrintNumberInWord, what output is displayed if number is 10 and no case matches?
34. How does a nested-if statement work when checking numbers 1 to 9 in PrintNumberInWord?
35. What is the advantage of using a switch statement over multiple if-else statements in PrintNumberInWord?
36. In the PrintDayInWord program, what day is printed when dayNumber is 0?
37. In PrintDayInWord, what output is displayed if dayNumber is 7?
38. Can you use a switch statement with integer values in Java?
39. What is the difference between using (number % 2 == 0) and (number % 2 != 0)?
40. Why must variable names be meaningful like numStudents instead of single letters like n?
41. What is indentation in Java code and why is it important for code readability?
42. In Java, what happens if an if condition evaluates to false and there is no else block?
43. Can an if statement contain another if statement inside it? What is it called?
44. What is the output of 49 >= 50 in Java? (true or false?)
45. What is the output of 50 >= 50 in Java? (true or false?)
46. In a switch statement, can two case labels have the exact same value? Why or why not?
47. How does the compiler know where an if block ends in Java?
48. What is the difference between relational operators (like > and <) and logical operators (like && and ||)?
49. In PrintNumberInWord, why is a default case useful when handling unexpected user inputs?
50. What is the output of System.out.println("The mark is " + 49)?
